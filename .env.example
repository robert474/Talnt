# Slack Bot Tokens (only needed for Slack bot, not web app)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# Claude API (optional, improves PDF parsing)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Web App Configuration (for cloud deployment)
PORT=5050
FLASK_DEBUG=false
